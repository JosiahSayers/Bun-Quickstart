name: CI
on:
  push:
    branches: [ "main" ]
  pull_request:
jobs:
  test:
    name: build-app
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup
        uses: ./.github/actions/setup
      - name: Run tests
        run: bun test
